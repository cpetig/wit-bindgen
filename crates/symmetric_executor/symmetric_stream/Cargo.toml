[package]
name = "symmetric_stream"
version.workspace = true
edition.workspace = true

[dependencies]
symmetric_executor = { path = ".." }
wit-bindgen = { package = "mini-bindgen", path = "../dummy-bindgen" }

[lib]
crate-type = ["cdylib"]

[features]
trace = []
