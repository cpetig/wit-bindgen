[package]
name = "symmetric_stream"
version.workspace = true
edition.workspace = true

[dependencies]
symmetric_executor = { path = ".." }
wit-bindgen = { package = "dummy-rt", path = "../dummy-rt" }

[lib]
crate-type = ["cdylib"]

[features]
trace = []
